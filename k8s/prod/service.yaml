apiVersion: v1
kind: Service
metadata:
  name: credit-scoring-api
  labels:
    app: credit-scoring-api
spec:
  type: NodePort
  selector:
    app: credit-scoring-api
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      nodePort: 30800
